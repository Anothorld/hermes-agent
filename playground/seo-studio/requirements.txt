fastapi>=0.115,<1
uvicorn[standard]>=0.30,<1
httpx>=0.27,<1
# WordPress draft export — reuses the operator's wordpress_mcp package.
# pyyaml reads WP creds from the profile config.yaml (mcp_servers.wordpress.env);
# beautifulsoup4 + requests are wordpress_mcp's own parser/client deps.
beautifulsoup4>=4.12,<5
requests>=2.31,<3
pyyaml>=6,<7
