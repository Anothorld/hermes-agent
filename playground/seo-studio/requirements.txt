fastapi>=0.115,<1
uvicorn[standard]>=0.30,<1
httpx>=0.27,<1
# WordPress draft export — reuses the operator's wordpress_mcp package.
# pyyaml reads WP creds from the profile config.yaml (mcp_servers.wordpress.env);
# beautifulsoup4 + requests are wordpress_mcp's own parser/client deps.
beautifulsoup4>=4.12,<5
requests>=2.31,<3
pyyaml>=6,<7
itsdangerous>=2.1,<3
# Markdown → HTML for WP export (GFM tables, lists, links). Without this,
# pipe tables land in WordPress as raw "| col | col |" paragraphs.
markdown>=3.5,<4
# Editorial Picks review fetch — read-only access to the magento2 review DB.
# mysql-connector-python is pure-python + supports ssl; chosen over pymysql for
# its broader GCP Cloud SQL connectivity story (serverCA path if needed later).
mysql-connector-python>=8.4,<9
# Test runner for tests/ — dev-only but kept in the main requirements so CI /
# `./start.sh` environments can run the suite without a second install step.
pytest>=8,<9
