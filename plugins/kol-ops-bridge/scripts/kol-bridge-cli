#!/usr/bin/env bash
# Stable bridge CLI entry — always python3 + absolute kol_bridge_tool.py path.
# Use this in gateway terminal commands (macOS has no bare `python` shim).
set -euo pipefail
SCRIPT_DIR="$(cd "$(dirname "$0")" && pwd)"
exec python3 "${SCRIPT_DIR}/kol_bridge_tool.py" "$@"
