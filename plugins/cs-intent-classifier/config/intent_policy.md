# Distilled intent policy rules.
# Populated by the T3 offline distillation cron (Phase 4). Starts empty for v1.
# Format: markdown rules with ADJUST:/REMOVE: prefixes for incremental updates.
# The classifier injects this block into the LLM prompt via build_policy_rules_block().
